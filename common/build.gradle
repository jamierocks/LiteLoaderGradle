apply plugin: 'java'
apply plugin: 'net.minecrell.vanilla.base'

apply from: file('../gradle/minecraft.gradle')

shadowJar {
    classifier ''
}
assemble.dependsOn shadowJar

sourceSets {
    main {
        java {
            srcDir '../LiteLoader/LiteLoader/trunk/java/common'
        }
    }
}